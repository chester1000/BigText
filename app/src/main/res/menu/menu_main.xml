<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

      <item android:id="@+id/buy_premium"
            android:title="Buy premium"
            android:icon="@drawable/ic_shop_white_24dp"
            app:showAsAction="always" />

      <item android:id="@+id/action_print"
            android:title="@string/action_print"
            android:icon="@drawable/ic_print_white_24dp"
            app:showAsAction="ifRoom" />

      <item android:id="@+id/action_settings"
            android:title="@string/action_settings"
            android:orderInCategory="100"
            app:showAsAction="never" />
</menu>
